<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <!--    <meta-data-->
<!--        android:name="com.google.android.geo.API_KEY"-->
<!--        android:value="AIzaSyDBUnPoR26SZzEKC49f3AxKYwKwuvgQ2Qs"/>-->
    <meta-data
        android:name="org.osmdroid.config"
        android:value="true"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TravelApp"
        tools:targetApi="31">
        <activity
            android:name=".Activity.ExplorerActivity"
            android:exported="true" />
        <activity
            android:name=".Activity.BookMarkActivity"
            android:exported="false" /> <!-- IntroActivity (Màn hình khởi động) -->
        <activity
            android:name=".Activity.IntroActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- MainActivity (Màn hình chính) -->
        <activity
            android:name=".Activity.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity="com.example.travelapp.MainActivity" /> <!-- LoginActivity -->
        <activity
            android:name=".Activity.LoginActivity"
            android:exported="true" /> <!-- SignupActivity (Thiếu, cần bổ sung) -->
        <activity
            android:name=".Activity.SignupActivity"
            android:exported="true" /> <!-- EditProfileActivity (Nếu có màn hình chỉnh sửa hồ sơ) -->
        <activity
            android:name=".Activity.EditProfileActivity"
            android:exported="false" /> <!-- TicketActivity -->
        <activity
            android:name=".Activity.TicketActivity"
            android:exported="false" /> <!-- DetailActivity -->
        <activity
            android:name=".Activity.Detailactivity"
            android:exported="true" />
        <activity
            android:name=".Activity.ProfileActivity"
            android:exported="true" />
    </application>

</manifest>